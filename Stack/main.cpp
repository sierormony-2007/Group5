#include <stack>
#include <iostream>
#include "stack.hpp"
using namespace std;


// stack<char> temp;
// int main(){
// string s = "{}[]{}";
// isValid(s);
// cout << (isValid(s) ? "Valid" : "Invalid") << endl;
//   return 0;
// }

int main(){
    Stack s;
    bool isBalance(){
      for(int i=0; i<s.length(); i++){
        if(s.[i]=='(' || s.[i]=='{' || s.[i]=='['){
          s[i].push();
        }else if(s.[i]=='(' || s.[i]=='{' || s.[i]=='['){

        }

        }
      }
    }
  